@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container d-flex flex-column justify-content-center align-items-center text-center min-vh-100 p-3">
    <h1 class="display-4 text-danger">
        <i class="bi bi-shield-lock-fill"></i> Acceso Denegado
    </h1>
    <p class="lead">No tienes permisos suficientes para acceder a esta sección.</p>
    <a class="btn btn-primary mt-3" asp-controller="Home" asp-action="Index">Volver al Inicio</a>
</div>
